# src/assets

Local static assets imported by components (images, svgs, fonts) live here.
Fonts (Fredoka, Inter) are loaded from Google Fonts in `index.html`; drop
self-hosted `.woff2` files here and swap to `@font-face` if you prefer to
bundle them.
